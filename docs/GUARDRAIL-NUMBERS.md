# Номера ограничителей: таблица псевдонимов

2026-08-20 ограничители перенумерованы так, чтобы номер совпадал с порядком
чтения `core/GUARDRAILS.md`. До этого номера раздавались в порядке появления
правил, а файл сгруппирован по темам, поэтому реестр читался
`G1 G2 G3 G4 G15 G16 G8 G17 G10 G18 G14 G9 G13 G11 G12 G7 G6 G5` — и ссылка
«см. G15» не давала читателю никакого представления, где искать.

**Что перенумеровано, а что нет.** Нормативные тексты — ядро, навыки, `docs/`,
`bin/`, тесты, `ATTRIBUTIONS.md`, `PLAN.md` — описывают правила такими, какие
они сейчас, и переведены на новые номера. Датированные записи о том, что было
сказано и сделано, — `CHANGELOG.md`, `TODOS.md`, `review/`, `bench/`,
`docs/GSTACK-ETHOS.md` — оставлены как есть: переписать их значило бы
подделать запись. Ссылки в них читаются через **старую** колонку этой таблицы.

| было | стало | правило |
|---|---|---|
| G1 | **G1** | Blast radius — радиус изменений |
| G2 | **G2** | Planning gate, by radius — гейт планирования |
| G3 | **G3** | Reading budget — бюджет чтения |
| G4 | **G4** | Exit-code proof — верификация по коду возврата |
| G15 | **G5** | Claimed limits need evidence — доказательства для заявленных ограничений |
| G16 | **G6** | Carried claims expire — перенесённые утверждения протухают |
| G8 | **G7** | Harness immutability and hygiene — неприкосновенность харнесса |
| G17 | **G8** | Resolve the target before destroying it — разрешение цели |
| G10 | **G9** | Destructive commands — терминальная безопасность |
| G18 | **G10** | Instructions come by channel — инструкции приходят по каналу |
| G14 | **G11** | Snapshot before risk — снапшот перед рискованным шагом |
| G9 | **G12** | Circuit breaker — предохранитель циклов |
| G13 | **G13** | Hypothesis ledger — журнал гипотез |
| G11 | **G14** | Checkpoints — чекпоинты |
| G12 | **G15** | Escalation — эскалация |
| G7 | **G16** | Role isolation — ролевая изоляция |
| G6 | **G17** | The change under review — что считается изменением |
| G5 | **G18** | UI states — матрица состояний интерфейса |

Обратный порядок, если под рукой новый номер, а прочитанный текст старый:

| стало | было | | стало | было | | стало | было |
|---|---|---|---|---|---|---|---|
| G1 | G1 | | G7 | G8 | | G13 | G13 |
| G2 | G2 | | G8 | G17 | | G14 | G11 |
| G3 | G3 | | G9 | G10 | | G15 | G12 |
| G4 | G4 | | G10 | G18 | | G16 | G7 |
| G5 | G15 | | G11 | G14 | | G17 | G6 |
| G6 | G16 | | G12 | G9 | | G18 | G5 |

**Один файл первый проход пропустил.** `NOTICE` ссылался на «G6» там, где речь
о накопительном радиусе изменений, — это старый номер, ставший **G17**;
исправлено 2026-08-20 после сверки. Пропущен он потому, что подстановка обходила
каталоги (`core/`, `skills/`, `docs/`, `bin/`, `tests/`) плюс два файла в корне,
названных поимённо, а `NOTICE` в этот список не попал. Нашлось сверкой каждого
файла с его состоянием до перенумерации: 42 файла совпали байт в байт с
ожидаемой подстановкой, три расходились по добавленным строкам, а `NOTICE` в
изменённых не значился вовсе, хотя ссылку на правило содержит.

**Чтобы это не повторилось,** `tests/test_docs.py` проверяет, что номера в
`core/GUARDRAILS.md` идут подряд от `G1` и без пропусков. Новое правило
дописывается в конец темы и получает следующий номер только там, где эта тема
кончается; вставка в середину означает перенумерацию и новую строку в этой
таблице.
